import { colors } from "@/lib/theme";
import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    avatar: {
        backgroundColor: colors.primitive.greyPurple,
        borderWidth: 1,
        borderColor: colors.primitive.greyPurple,
        width: 56,
        height: 56,
        borderRadius: 28,
    },
})